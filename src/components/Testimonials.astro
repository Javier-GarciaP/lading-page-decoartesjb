---
import Section from "./Section.astro";

const opinions = [
  {
    fecha: "Diciembre 2024",
    nombre: "Berlkis Rosales",
    texto:
      "Jonathan es un decorador bastante profesional. Es intachable todo lo que puede realizar en un evento, ya sea un matrimonio, empresarial o muy privado. Siempre se ha destacado con sus detalles.",
    rating: 5,
    rol: "Cliente Bodas",
  },
  {
    fecha: "Enero 2025",
    nombre: "María Ruiz",
    texto:
      "Jonathan no tengo más palabras para felicitarte y elogiarte. Tus decoraciones son increíbles, mágicas y soñadas. Cada arreglo, cada detalle llena de armonía los espacios. Eres un artista.",
    rating: 5,
    rol: "Cliente Eventos",
  },
  {
    fecha: "Febrero 2025",
    nombre: "Margarita Ruiz",
    texto:
      "Jonathan, gracias por tu gran profesionalismo y excelente gusto en la decoración. Tus montajes son perfectos, bien trabajados y con resultados maravillosos. Cada puesta en escena es una sorpresa.",
    rating: 5,
    rol: "Cliente Corporativo",
  },
];
---

<Section id="opiniones">
  <div class="relative py-12 overflow-hidden">
    <!-- Background Elements -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
      <div
        class="absolute top-20 right-10 w-96 h-96 bg-primary/5 rounded-full blur-3xl"
      >
      </div>
      <div
        class="absolute bottom-20 left-10 w-80 h-80 bg-amber-500/5 rounded-full blur-3xl"
      >
      </div>
    </div>

    <div class="max-w-7xl mx-auto px-6 relative z-10">
      <!-- Header -->
      <div class="text-center mb-12 space-y-4">
        <div
          class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary/10 border border-primary/20 backdrop-blur-sm"
        >
          <svg
            class="w-4 h-4 text-primary"
            fill="currentColor"
            viewBox="0 0 20 20"
          >
            <path
              d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"
            ></path>
          </svg>
          <span
            class="text-sm font-semibold text-primary uppercase tracking-wider"
            >Testimonios</span
          >
        </div>

        <h2
          class="text-3xl md:text-5xl font-heading font-bold text-white leading-tight"
        >
          Lo Que Dicen
          <span
            class="bg-gradient-to-r from-primary via-amber-300 to-yellow-400 bg-clip-text text-transparent"
          >
            Nuestros Clientes
          </span>
        </h2>
        <p class="text-secondary max-w-2xl mx-auto text-lg">
          La satisfacción de nuestros clientes es nuestra mayor recompensa. Lee
          lo que tienen que decir sobre su experiencia.
        </p>
      </div>

      <!-- Testimonials Grid -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        {
          opinions.map((opinion, index) => (
            <div
              class="testimonial-card group relative flex flex-col p-5 rounded-3xl bg-gradient-to-br from-white/[0.07] to-white/[0.02] border border-white/10 backdrop-blur-xl hover:border-primary/30 transition-all duration-500 hover:shadow-2xl hover:shadow-primary/10 hover:-translate-y-2"
              data-index={index}
            >
              {/* Gradient Accent Line */}
              <div class="absolute top-0 left-0 right-0 h-1 bg-gradient-to-r from-transparent via-primary to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 rounded-t-3xl" />

              {/* Quote Icon */}
              <div class="absolute top-6 right-6 w-10 h-10 rounded-2xl bg-gradient-to-br from-primary/10 to-amber-500/10 flex items-center justify-center opacity-50 group-hover:opacity-100 transition-opacity duration-300">
                <svg
                  class="w-6 h-6 text-primary"
                  fill="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10h-9.983zm-14.017 0v-7.391c0-5.704 3.748-9.57 9-10.609l.996 2.151c-2.433.917-3.996 3.638-3.996 5.849h3.983v10h-9.983z" />
                </svg>
              </div>

              {/* Stars */}
              <div class="flex gap-1 mb-4">
                {[...Array(opinion.rating)].map(() => (
                  <svg
                    class="w-5 h-5 text-primary"
                    fill="currentColor"
                    viewBox="0 0 20 20"
                  >
                    <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
                  </svg>
                ))}
              </div>

              {/* Testimonial Text */}
              <p class="text-secondary leading-relaxed mb-6 flex-grow text-base">
                "{opinion.texto}"
              </p>

              {/* Divider */}
              <div class="h-px bg-gradient-to-r from-transparent via-white/10 to-transparent mb-6" />

              {/* Author Info */}
              <div class="flex items-center gap-4">
                <div class="w-10 h-10 rounded-full bg-gradient-to-br from-primary to-amber-400 flex items-center justify-center text-white font-bold text-lg shadow-lg">
                  {opinion.nombre.charAt(0)}
                </div>
                <div class="flex-1">
                  <h4 class="text-white font-semibold text-lg">
                    {opinion.nombre}
                  </h4>
                  <p class="text-xs text-secondary uppercase tracking-wider">
                    {opinion.rol}
                  </p>
                </div>
                <div class="text-right">
                  <p class="text-xs text-primary/60 uppercase tracking-wider">
                    {opinion.fecha}
                  </p>
                </div>
              </div>

              {/* Hover Glow Effect */}
              <div class="absolute inset-0 rounded-3xl bg-gradient-to-br from-primary/0 to-primary/0 group-hover:from-primary/5 group-hover:to-transparent transition-all duration-500 pointer-events-none" />
            </div>
          ))
        }
      </div>

      <!-- Bottom Stats -->
      <div class="mt-16 grid grid-cols-3 gap-8 max-w-3xl mx-auto">
        <div class="text-center">
          <div class="text-4xl font-bold text-primary mb-2">500+</div>
          <div class="text-sm text-secondary uppercase tracking-wider">
            Clientes Felices
          </div>
        </div>
        <div class="text-center border-x border-white/10">
          <div class="text-4xl font-bold text-primary mb-2">5.0</div>
          <div class="text-sm text-secondary uppercase tracking-wider">
            Calificación
          </div>
        </div>
        <div class="text-center">
          <div class="text-4xl font-bold text-primary mb-2">98%</div>
          <div class="text-sm text-secondary uppercase tracking-wider">
            Recomendación
          </div>
        </div>
      </div>
    </div>
  </div>
</Section>

<script>
  // Stagger animation for testimonial cards
  const initTestimonialCards = () => {
    const cards = document.querySelectorAll(".testimonial-card");

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const element = entry.target as HTMLElement;
            const index = parseInt(element.getAttribute("data-index") || "0");

            setTimeout(() => {
              element.style.opacity = "1";
              element.style.transform = "translateY(0)";
            }, index * 150);

            observer.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.1 },
    );

    cards.forEach((card) => {
      const element = card as HTMLElement;
      element.style.opacity = "0";
      element.style.transform = "translateY(30px)";
      element.style.transition = "opacity 0.6s ease, transform 0.6s ease";
      observer.observe(card);
    });
  };

  // Initialize
  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", initTestimonialCards);
  } else {
    initTestimonialCards();
  }
</script>

<style>
  .testimonial-card {
    position: relative;
  }
</style>
