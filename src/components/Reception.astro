---
const { title: titulo } = Astro.props;
const hayTitulo = titulo ? true : false;
import Section from "./Section.astro";
---

<Section>
  <header
    class="relative w-full min-h-screen bg-[url('/fondo.jpg')] bg-cover bg-center text-white"
  >
    <!-- Capa oscura -->
    <div class="absolute inset-0 bg-black/85"></div>

    <div class="relative z-10 min-h-screen flex flex-col px-6 md:px-10 py-5">
      <!-- Navbar -->
      <nav
        class="w-full flex justify-between items-center font-sans"
        aria-label="Navegación principal"
      >
        <h4 class="font-primary text-primary text-3xl md:text-4xl">
          Decoartesjb
        </h4>

        <ul class="flex gap-6 items-center font-p text-m-ancor md:text-ancor">
          <li class="hidden md:block"><a href="/#sobre-mi" class="hover:text-primary transition-all">Sobre mí</a></li>
          <li class="hidden md:block"><a href="/#opiniones" class="hover:text-primary transition-all">Reseñas</a></li>
          <li><a href="/#servicios" class="hover:text-primary transition-all">Servicios</a></li>
          <li><a href="/galery" class="hover:text-primary transition-all">Galería</a></li>
        </ul>

        <a
          href="#contact"
          class="hidden text-m-b md:text-b font-p md:block px-6 md:px-8 py-2 bg-primary rounded-lg font-bold text-white hover:bg-opacity-80 transition"
        >
          Contáctanos
        </a>
      </nav>

      <!-- Mostrar título si se pasa como prop, de lo contrario mostrar contenido principal -->
      {hayTitulo ? (
        <div class="flex flex-col justify-center items-center flex-grow text-center px-4">
          <h1 class="font-primary text-m-h1 md:text-h1 leading-tight">
            {titulo}
          </h1>
        </div>
      ) : (
        <div class="flex flex-col justify-center items-center flex-grow text-center px-4">
          <h2 class="font-primary text-m-h1 md:text-h1 leading-tight">
            Transformamos tus sueños en <br />
            <span class="text-primary">Eventos Inolvidables</span>
          </h2>
          <p class="text-m-p md:text-p font-p mt-4">
            ¿Imaginas un evento que supere todas tus expectativas?<br />
            15+ años de experiencia diseñando bodas, eventos corporativos, cumpleaños y más.
          </p>
          <p class="text-m-p md:text-p font-p mt-2">
            ¡Contáctame y hagamos algo especial juntos!
          </p>

          <a
            href="#contact"
            class="block text-m-b md:text-b font-p md:hidden px-6 md:px-8 py-3 bg-primary rounded-lg font-bold hover:bg-opacity-80 transition mt-8"
          >
            Reserva con nosotros
          </a>
        </div>
      )}
    </div>
  </header>
</Section>