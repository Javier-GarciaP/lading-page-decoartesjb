---
import Section from "./Section.astro";

const PROCESS_STEPS = [
  {
    step: "01",
    title: "Conexión",
    description: "Nos reunimos para escuchar tus sueños. Entendemos tu visión, gustos y la esencia que deseas transmitir en tu evento.",
    icon: "chat"
  },
  {
    step: "02",
    title: "Diseño",
    description: "Creamos una propuesta visual única. Bocetos, paletas de colores y selección de materiales para dar vida a la idea.",
    icon: "pencil"
  },
  {
    step: "03",
    title: "Magia",
    description: "La ejecución impecable. Nuestro equipo transforma el espacio cuidando cada flor, cada luz y cada detalle.",
    icon: "sparkles"
  },
  {
    step: "04",
    title: "Celebración",
    description: "El momento de disfrutar. Todo está listo para que tú y tus invitados vivan una experiencia inolvidable.",
    icon: "cheers"
  }
];
---

<Section id="proceso">
  <div class="relative py-24 overflow-hidden">
    <!-- Background Elements -->
    <div class="absolute inset-0 pointer-events-none">
      <div class="absolute top-0 right-1/4 w-96 h-96 bg-primary/5 rounded-full blur-[100px]"></div>
      <div class="absolute bottom-0 left-1/4 w-96 h-96 bg-amber-500/5 rounded-full blur-[100px]"></div>
    </div>

    <div class="max-w-7xl mx-auto px-6 relative z-10">
      
      <!-- Header -->
      <div class="text-center mb-20 space-y-4">
        <span class="text-primary font-semibold tracking-[0.2em] text-sm uppercase opacity-80">Cómo trabajamos</span>
        <h2 class="text-3xl md:text-5xl font-heading font-bold text-white">
          El Proceso 
          <span class="text-transparent bg-clip-text bg-gradient-to-r from-primary via-amber-200 to-yellow-500">
            Creativo
          </span>
        </h2>
        <p class="text-secondary max-w-2xl mx-auto text-lg pt-4">
          De la imaginación a la realidad. Un camino cuidado al detalle para garantizar la excelencia.
        </p>
      </div>

      <!-- Timeline Process -->
      <div class="relative">
        <!-- Connecting Line (Desktop) -->
        <div class="hidden md:block absolute top-[60px] left-0 w-full h-0.5 bg-gradient-to-r from-transparent via-primary/30 to-transparent"></div>

        <div class="grid grid-cols-1 md:grid-cols-4 gap-12 relative">
          {PROCESS_STEPS.map((step, index) => (
            <div class="group relative flex flex-col items-center text-center space-y-6">
              
              <!-- Icon/Number Bubble -->
              <div class="relative">
                <div class="w-32 h-32 rounded-full border border-white/10 bg-white/5 backdrop-blur-sm flex items-center justify-center relative z-10 group-hover:border-primary/50 group-hover:bg-primary/10 transition-all duration-500 shadow-xl shadow-black/20 group-hover:shadow-primary/20 group-hover:scale-110">
                  <div class="absolute inset-2 rounded-full border border-dashed border-white/20 group-hover:border-primary/30 animate-spin-slow"></div>
                  
                  <span class="text-4xl">
                    {step.icon === 'chat' && '☕'}
                    {step.icon === 'pencil' && '🎨'}
                    {step.icon === 'sparkles' && '✨'}
                    {step.icon === 'cheers' && '🥂'}
                  </span>
                  
                  <!-- Step Number Badge -->
                  <div class="absolute -top-2 -right-2 w-10 h-10 rounded-full bg-gradient-to-br from-primary to-amber-500 flex items-center justify-center text-white font-bold text-sm shadow-lg">
                    {step.step}
                  </div>
                </div>
              </div>

              <!-- Content -->
              <div class="space-y-3 px-2">
                <h3 class="text-xl font-bold text-white group-hover:text-primary transition-colors duration-300">
                  {step.title}
                </h3>
                <p class="text-secondary text-sm leading-relaxed opacity-80 group-hover:opacity-100 transition-opacity">
                  {step.description}
                </p>
              </div>

              <!-- Connecting Line (Mobile) -->
              {index !== PROCESS_STEPS.length - 1 && (
                <div class="md:hidden w-0.5 h-12 bg-gradient-to-b from-primary/30 to-transparent my-2"></div>
              )}
            </div>
          ))}
        </div>
      </div>

      <!-- Bottom CTA -->
      <div class="flex justify-center mt-24">
        <a 
          href="#contact" 
          class="group relative px-10 py-5 bg-transparent overflow-hidden rounded-full transition-all duration-500 hover:scale-105 active:scale-95 shadow-2xl shadow-primary/20 hover:shadow-primary/40"
        >
          {/* Main Background with Glass Effect */}
          <div class="absolute inset-0 bg-gradient-to-r from-primary/20 via-amber-500/20 to-primary/20 backdrop-blur-md border border-primary/40 rounded-full transition-all duration-500 group-hover:from-primary group-hover:to-amber-500"></div>
          
          {/* Animated Shine Effect */}
          <div class="absolute inset-0 w-full h-full transform -skew-x-[45deg] bg-gradient-to-r from-transparent via-white/30 to-transparent -translate-x-[150%] transition-transform duration-700 ease-in-out group-hover:translate-x-[150%]"></div>
          
          {/* Content */}
          <span class="relative flex items-center gap-4 text-primary font-bold tracking-[0.1em] uppercase text-sm group-hover:text-white transition-colors duration-500">
            Comenzar mi proyecto
            <div class="flex items-center justify-center w-6 h-6 rounded-full bg-primary/20 group-hover:bg-white/20 transition-colors">
              <svg class="w-3.5 h-3.5 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
              </svg>
            </div>
          </span>
        </a>
      </div>

    </div>
  </div>
</Section>

<style>
  @keyframes spin-slow {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }
  .animate-spin-slow {
    animation: spin-slow 12s linear infinite;
  }
</style>
