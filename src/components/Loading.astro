<div class:list={["elegant-spinner", Astro.props.class]}>
  <div class="ring"></div>
  <div class="ring"></div>
  <div class="dot"></div>
</div>

<style>
  .elegant-spinner {
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
    /* Default size, can be overridden by Tailwind classes passed in props */
  }

  .ring {
    position: absolute;
    width: 100%;
    height: 100%;
    border: 2px solid transparent;
    border-top: 2px solid var(--color-primary);
    border-radius: 50%;
    animation: spin 2s cubic-bezier(0.5, 0, 0.5, 1) infinite;
  }

  .ring:nth-child(2) {
    width: 80%;
    height: 80%;
    border-top: 2px solid var(--color-primary-dark);
    animation: spin 1.5s cubic-bezier(0.5, 0, 0.5, 1) infinite reverse;
  }

  .dot {
    position: absolute;
    width: 15%;
    height: 15%;
    background-color: var(--color-primary);
    border-radius: 50%;
    box-shadow: 0 0 10px var(--color-primary);
    animation: pulse 1s ease-in-out infinite alternate;
  }

  @keyframes spin {
    0% {
      transform: rotate(0deg);
    }
    100% {
      transform: rotate(360deg);
    }
  }

  @keyframes pulse {
    0% {
      opacity: 0.5;
      transform: scale(0.8);
    }
    100% {
      opacity: 1;
      transform: scale(1.1);
    }
  }
</style>
