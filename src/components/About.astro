---
import Section from "./Section.astro";
---

<Section id="sobre-mi">
  <section class="relative flex justify-center py-10 md:py-12 overflow-hidden">
    <!-- Animated Background Elements -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
      <!-- Floating Orbs -->
      <div
        class="floating-orb absolute top-20 left-10 w-32 h-32 bg-primary/20 rounded-full blur-3xl animate-float-slow"
      >
      </div>
      <div
        class="floating-orb absolute bottom-20 right-20 w-40 h-40 bg-amber-500/20 rounded-full blur-3xl animate-float-slower"
      >
      </div>
      <div
        class="floating-orb absolute top-1/2 left-1/4 w-24 h-24 bg-yellow-500/15 rounded-full blur-2xl animate-float-medium"
      >
      </div>

      <!-- Animated Particles -->
      <div id="particles-container" class="absolute inset-0"></div>
    </div>

    <div
      class="flex flex-col-reverse md:flex-row items-center gap-8 max-w-6xl w-full px-6 relative z-10"
    >
      <!-- Narrative Section -->
      <div class="text-left w-full md:w-1/2 space-y-6">
        <!-- Badge -->
        <div
          class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary/10 border border-primary/20 backdrop-blur-sm animate-fade-in-up"
        >
          <svg
            class="w-4 h-4 text-primary"
            fill="currentColor"
            viewBox="0 0 20 20"
          >
            <path
              d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"
            ></path>
          </svg>
          <span
            class="text-sm font-semibold text-primary uppercase tracking-wider"
            >Sobre Nosotros</span
          >
        </div>

        <!-- Title -->
        <h2
          class="font-heading text-3xl md:text-4xl font-bold tracking-tight leading-tight animate-fade-in-up"
          style="animation-delay: 0.1s"
        >
          ¿Quién <span
            class="bg-gradient-to-r from-primary via-amber-300 to-yellow-400 bg-clip-text text-transparent"
            >soy?</span
          >
        </h2>

        <!-- Description -->
        <div
          class="space-y-4 text-secondary leading-relaxed text-lg animate-fade-in-up"
          style="animation-delay: 0.2s"
        >
          <p class="text-white/90">
            Soy <span class="text-primary font-semibold">Jonathan</span>,
            emprendedor con más de
            <span class="inline-flex items-center gap-1">
              <span class="experience-counter text-primary font-bold text-xl"
                >15</span
              >
              <span class="text-primary font-bold">años</span>
            </span>
            creando atmósferas únicas.
          </p>
          <p>
            Transformo espacios en escenarios memorables para bodas y eventos
            corporativos, diseñando cada detalle con pasión y precisión.
          </p>
          <p>
            Mi compromiso es convertir tu visión en realidad, superando
            expectativas y creando momentos que perduran en la memoria.
          </p>
        </div>

        <!-- Features List -->
        <div
          class="grid grid-cols-2 gap-4 pt-4 animate-fade-in-up"
          style="animation-delay: 0.3s"
        >
          <div class="flex items-center gap-3 group">
            <div
              class="w-10 h-10 rounded-lg bg-primary/10 flex items-center justify-center group-hover:bg-primary/20 transition-colors"
            >
              <svg
                class="w-5 h-5 text-primary"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M5 13l4 4L19 7"></path>
              </svg>
            </div>
            <span class="text-white/80 text-sm font-medium">Diseño Único</span>
          </div>
          <div class="flex items-center gap-3 group">
            <div
              class="w-10 h-10 rounded-lg bg-primary/10 flex items-center justify-center group-hover:bg-primary/20 transition-colors"
            >
              <svg
                class="w-5 h-5 text-primary"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M5 13l4 4L19 7"></path>
              </svg>
            </div>
            <span class="text-white/80 text-sm font-medium"
              >Atención Detalle</span
            >
          </div>
          <div class="flex items-center gap-3 group">
            <div
              class="w-10 h-10 rounded-lg bg-primary/10 flex items-center justify-center group-hover:bg-primary/20 transition-colors"
            >
              <svg
                class="w-5 h-5 text-primary"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M5 13l4 4L19 7"></path>
              </svg>
            </div>
            <span class="text-white/80 text-sm font-medium"
              >Calidad Premium</span
            >
          </div>
          <div class="flex items-center gap-3 group">
            <div
              class="w-10 h-10 rounded-lg bg-primary/10 flex items-center justify-center group-hover:bg-primary/20 transition-colors"
            >
              <svg
                class="w-5 h-5 text-primary"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M5 13l4 4L19 7"></path>
              </svg>
            </div>
            <span class="text-white/80 text-sm font-medium">Experiencia</span>
          </div>
        </div>
      </div>

      <!-- Visual Section with 3D Effect -->
      <div
        class="relative w-full md:w-1/2 flex justify-center animate-fade-in-up"
        style="animation-delay: 0.4s"
      >
        <div class="relative w-56 md:w-72 aspect-square" id="image-container">
          <!-- Profile Image -->
          <div class="absolute inset-0 z-0">
            <!-- Glow Effect backing -->
            <div
              class="absolute inset-0 bg-gradient-to-br from-primary/20 via-transparent to-amber-500/20 rounded-full blur-3xl opacity-60"
            >
            </div>
            <!-- Image Container with Modern Styling -->
            <div class="relative w-full h-full group">
              <div
                class="absolute inset-0 bg-gradient-to-br from-primary via-amber-400 to-yellow-500 rounded-3xl opacity-75 group-hover:opacity-100 transition-opacity duration-500"
              >
              </div>
              <div class="absolute inset-1 rounded-3xl overflow-hidden">
                <img
                  src="/perfil/perfil-2.jpg"
                  alt="Decorador Profesional Jonathan"
                  class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
                  loading="lazy"
                />
                <div
                  class="absolute inset-0 bg-gradient-to-t from-black/40 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500"
                >
                </div>
              </div>
            </div>
          </div>

          <!-- Floating Badges -->
          <div
            class="absolute -bottom-6 -left-6 glass px-5 py-3 rounded-2xl flex items-center gap-3 shadow-2xl shadow-primary/20 animate-float border border-white/10 z-10"
          >
            <div
              class="w-12 h-12 rounded-xl bg-gradient-to-br from-primary to-amber-500 flex items-center justify-center"
            >
              <span class="text-2xl">✨</span>
            </div>
            <div>
              <div class="text-xs text-secondary uppercase tracking-wider">
                Experiencia
              </div>
              <div class="text-lg font-bold text-white">15+ Años</div>
            </div>
          </div>

          <div
            class="absolute -top-6 -right-6 glass px-5 py-3 rounded-2xl flex items-center gap-3 shadow-2xl shadow-primary/20 animate-float-delayed border border-white/10 z-10"
          >
            <div
              class="w-12 h-12 rounded-xl bg-gradient-to-br from-amber-500 to-yellow-500 flex items-center justify-center"
            >
              <span class="text-2xl">🎨</span>
            </div>
            <div>
              <div class="text-xs text-secondary uppercase tracking-wider">
                Eventos
              </div>
              <div class="text-lg font-bold text-white">500+</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</Section>

<script>
  // Counter Animation
  const counter = document.querySelector(".experience-counter");
  if (counter) {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          let current = 0;
          const target = 15;
          const duration = 2000;
          const increment = target / (duration / 16);

          const animate = () => {
            current += increment;
            if (current < target) {
              counter.textContent = Math.floor(current).toString();
              requestAnimationFrame(animate);
            } else {
              counter.textContent = target.toString();
            }
          };

          animate();
          observer.disconnect();
        }
      });
    });
    observer.observe(counter);
  }

  // Floating Particles
  const createParticles = () => {
    const container = document.getElementById("particles-container");
    if (!container) return;

    const particleCount = 20;

    for (let i = 0; i < particleCount; i++) {
      const particle = document.createElement("div");
      particle.className = "particle";

      const size = Math.random() * 4 + 2;
      const left = Math.random() * 100;
      const animationDuration = Math.random() * 10 + 10;
      const delay = Math.random() * 5;

      particle.style.cssText = `
        position: absolute;
        width: ${size}px;
        height: ${size}px;
        background: radial-gradient(circle, rgba(212, 175, 55, 0.8), transparent);
        border-radius: 50%;
        left: ${left}%;
        bottom: -10px;
        animation: float-up ${animationDuration}s linear ${delay}s infinite;
        pointer-events: none;
      `;

      container.appendChild(particle);
    }
  };

  // Initialize
  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", createParticles);
  } else {
    createParticles();
  }
</script>

<style>
  @keyframes float-up {
    0% {
      transform: translateY(0) translateX(0);
      opacity: 0;
    }
    10% {
      opacity: 1;
    }
    90% {
      opacity: 1;
    }
    100% {
      transform: translateY(-100vh) translateX(20px);
      opacity: 0;
    }
  }

  @keyframes float-slow {
    0%,
    100% {
      transform: translateY(0) translateX(0);
    }
    50% {
      transform: translateY(-20px) translateX(10px);
    }
  }

  @keyframes float-slower {
    0%,
    100% {
      transform: translateY(0) translateX(0);
    }
    50% {
      transform: translateY(-30px) translateX(-15px);
    }
  }

  @keyframes float-medium {
    0%,
    100% {
      transform: translateY(0) translateX(0);
    }
    50% {
      transform: translateY(-25px) translateX(12px);
    }
  }

  @keyframes spin-slow {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  @keyframes spin-reverse {
    from {
      transform: rotate(360deg);
    }
    to {
      transform: rotate(0deg);
    }
  }

  .animate-float-slow {
    animation: float-slow 8s ease-in-out infinite;
  }

  .animate-float-slower {
    animation: float-slower 12s ease-in-out infinite;
  }

  .animate-float-medium {
    animation: float-medium 10s ease-in-out infinite;
  }

  .animate-spin-slow {
    animation: spin-slow 20s linear infinite;
  }

  .animate-spin-reverse {
    animation: spin-reverse 15s linear infinite;
  }

  .animate-float {
    animation: float-slow 6s ease-in-out infinite;
  }

  .animate-float-delayed {
    animation: float-slow 6s ease-in-out infinite;
    animation-delay: 1s;
  }

  .shine-effect {
    transform: translateX(-100%) rotate(45deg);
    transition: transform 0.6s ease;
  }

  .image-3d:hover .shine-effect {
    transform: translateX(100%) rotate(45deg);
  }

  .glass {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
  }
</style>
